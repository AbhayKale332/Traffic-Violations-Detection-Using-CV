class Settings:
    PROJECT_NAME: str = "City-Scale Vehicle Pursuit Network"
    VERSION: str = "0.1.0"
    API_V1_STR: str = "/api/v1"

# Create a global instance of the settings
settings = Settings()